#!/usr/bin/env node
'use strict';

process.bin = process.title = 'gg';

var gg = require('../lib/gg.js');
var cli = require('cli');

cli.parse(null, ['i', 'init', 'cl', 'clone', 'a', 'aa', 'add', 'c', 'commit', 'p', 'push', 'pl', 'pull', 'f', 'fetch', 's', 'status', 'l', 'log']);

switch (cli.command) {
	// Initialize.
	case 'i':
	case 'init':
		gg.init();
		break;

	case 'cl':
	case 'clone':
		var repository = cli.args[0];
		gg.clone(repository);
		break;

	// Add.
	case 'a':
	case 'aa': // Short for 'add all'.
	case 'add':
		gg.addAll();
		break;

	// Commit.
	case 'c':
	case 'commit':
		var message = cli.args.join(' ');
		gg.commit(message);
		break;

	// Push.
	case 'p':
	case 'push':
		gg.push();
		break;

	// Pull.
	case 'pl':
	case 'pull':
		gg.pull();
		break;

	// Fetch.
	case 'f':
	case 'fetch':
		gg.fetch();
		break;

	// Status.
	case 's':
	case 'status':
		gg.status();
		break;

	// Log.
	case 'l':
	case 'log':
		gg.log();
		break;
}
